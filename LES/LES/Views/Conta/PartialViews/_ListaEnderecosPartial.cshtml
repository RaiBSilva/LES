@model IList<DetalhesEnderecoModel>

    <dl>
        @foreach (var end in Model)
        {
            <partial name="../Conta/PartialViews/_DlEnderecoPartial.cshtml" for="@end" />
        }
    </dl>